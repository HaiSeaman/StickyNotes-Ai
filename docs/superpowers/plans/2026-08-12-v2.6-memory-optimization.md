# V2.6 内存优化（封面缩略图 + 后台节流）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 通过封面缩略图（96×96）与后台节流修复，将音乐 tab 激活时 renderer 内存从实测 447M 降至 ≤320M、任务管理器单条目从 ~450M 降至 ~330M，UI 视觉零变化。

**架构：** 主进程 `music:read-metadata` 提取封面时用 `nativeImage` 生成 96×96 JPEG 缩略图存 `covers/thumb/`，新增 `music:ensure-thumbs` IPC 幂等补齐已有封面；渲染层音乐列表项改用缩略图 URL（`inferThumbPath` 推断 + onerror 两级回退），控制条大封面保持原图。另将主窗口 `backgroundThrottling: false` 恢复默认 true（后台定时器被 Chromium 节流），按验证清单确认闹钟/音乐/时钟不受影响。

**技术栈：** Electron 42、TypeScript、nativeImage、vitest（node 环境单测）、CDP + PowerShell（集成测量）。

**设计文档：** `docs/superpowers/specs/2026-08-12-v2.6-memory-optimization-design.md`

---

## 文件结构

| 文件 | 职责 | 动作 |
|---|---|---|
| `src/main/lib/coverThumbs.ts` | 缩略图路径纯函数（`thumbPathFor`），无 electron 依赖可单测 | 创建 |
| `tests/coverThumbs.test.ts` | `thumbPathFor` 路径映射单测 | 创建 |
| `src/renderer/lib/rendererUtils.ts` | 渲染层 `inferThumbPath` 纯函数（无 path 模块的 Windows 路径推断） | 修改 |
| `tests/rendererUtils.test.ts` | `inferThumbPath` 单测 | 创建 |
| `src/main/ipc/index.ts` | read-metadata 生成缩略图返回 `thumbPath`；新增 `music:ensure-thumbs` handler | 修改 |
| `src/preload/preload.ts` | 暴露 `musicEnsureThumbs` | 修改 |
| `src/renderer/tabs/musicTab.ts` | 列表项缩略图 URL + onerror 两级回退 + init 触发补齐 | 修改 |
| `src/main/main.ts` | 删除 `backgroundThrottling: false` | 修改 |
| `.reasonix/tmp/cdp.mjs` | 基线测量脚本（复用，不入 git） | 复用 |

---

### 任务 1：主进程缩略图路径纯函数（TDD）

**文件：**
- 创建：`src/main/lib/coverThumbs.ts`
- 测试：`tests/coverThumbs.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
// tests/coverThumbs.test.ts
import { describe, it, expect } from 'vitest';
import { thumbPathFor } from '../src/main/lib/coverThumbs.js';

describe('thumbPathFor', () => {
    it('将 covers/<name>.ext 映射为 covers/thumb/<name>.jpg', () => {
        expect(thumbPathFor('C:\\Users\\xi\\AppData\\Roaming\\便签\\music\\covers\\34b41d6217f743473e0db822f0ed9779.jpg'))
            .toBe('C:\\Users\\xi\\AppData\\Roaming\\便签\\music\\covers\\thumb\\34b41d6217f743473e0db822f0ed9779.jpg');
    });
    it('支持 .png/.webp 扩展名且统一输出 .jpg', () => {
        expect(thumbPathFor('C:\\x\\covers\\a.png')).toBe('C:\\x\\covers\\thumb\\a.jpg');
        expect(thumbPathFor('C:\\x\\covers\\a.webp')).toBe('C:\\x\\covers\\thumb\\a.jpg');
    });
    it('无扩展名时按原样加 .jpg', () => {
        expect(thumbPathFor('C:\\x\\covers\\abc')).toBe('C:\\x\\covers\\thumb\\abc.jpg');
    });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run tests/coverThumbs.test.ts`
预期：FAIL，报错 "Cannot find module '../src/main/lib/coverThumbs.js'"

- [ ] **步骤 3：编写实现代码**

```ts
// src/main/lib/coverThumbs.ts
/* ==================== coverThumbs.ts ====================
 * 音乐封面缩略图路径工具（纯函数，无 electron 依赖，可单测）。
 * 约定：原图存 covers/<sha256>.<ext>，缩略图存 covers/thumb/<sha256>.jpg（固定 jpg）。
 * ==================================================== */
import path from 'path';

/** covers/<name>.<ext> → covers/thumb/<name>.jpg */
export function thumbPathFor(coverPath: string): string {
    const dir = path.dirname(coverPath);
    const base = path.basename(coverPath).replace(/\.[^.]+$/, '');
    return path.join(dir, 'thumb', base + '.jpg');
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run tests/coverThumbs.test.ts`
预期：PASS（3 个用例全过）

- [ ] **步骤 5：Commit**

```bash
git add src/main/lib/coverThumbs.ts tests/coverThumbs.test.ts
git commit -m "feat: add cover thumbnail path helper with tests"
```

---

### 任务 2：渲染层缩略图路径推断函数（TDD）

**文件：**
- 修改：`src/renderer/lib/rendererUtils.ts`
- 测试：`tests/rendererUtils.test.ts`

- [ ] **步骤 1：编写失败的测试**

```ts
// tests/rendererUtils.test.ts
import { describe, it, expect } from 'vitest';
import { inferThumbPath } from '../src/renderer/lib/rendererUtils.js';

describe('inferThumbPath', () => {
    it('将 Windows 路径 covers → covers/thumb 并统一 .jpg', () => {
        expect(inferThumbPath('C:\\Users\\xi\\AppData\\Roaming\\便签\\music\\covers\\a1b2.jpg'))
            .toBe('C:\\Users\\xi\\AppData\\Roaming\\便签\\music\\covers\\thumb\\a1b2.jpg');
    });
    it('支持正斜杠分隔', () => {
        expect(inferThumbPath('C:/Users/xi/music/covers/x.png'))
            .toBe('C:/Users/xi/music/covers/thumb/x.jpg');
    });
    it('路径不含 covers 标记时返回 null', () => {
        expect(inferThumbPath('C:\\Users\\xi\\music\\a.jpg')).toBeNull();
        expect(inferThumbPath('')).toBeNull();
        expect(inferThumbPath(null as any)).toBeNull();
    });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run tests/rendererUtils.test.ts`
预期：FAIL，报错 "inferThumbPath is not a function"

- [ ] **步骤 3：编写实现代码**

在 `src/renderer/lib/rendererUtils.ts` 文件末尾追加：

```ts
/**
 * 从封面原图路径推断缩略图路径（covers/<name>.<ext> → covers/thumb/<name>.jpg）。
 * 浏览器渲染层无 node path 模块，用字符串处理（兼容 \ 与 / 分隔符）。
 * 推断失败（路径不含 covers 标记）返回 null，由调用方回退原图。
 */
export function inferThumbPath(coverPath: string): string | null {
    if (!coverPath || typeof coverPath !== 'string') return null;
    const lower = coverPath.toLowerCase();
    const marker = '\\covers\\';
    const idx = lower.lastIndexOf(marker);
    if (idx < 0) return null;
    const head = coverPath.slice(0, idx + marker.length); // 含 ...\covers\
    const base = coverPath.slice(idx + marker.length).replace(/\.[^.]+$/, '');
    return head + 'thumb\\' + base + '.jpg';
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run tests/rendererUtils.test.ts`
预期：PASS（3 个用例全过）

- [ ] **步骤 5：Commit**

```bash
git add src/renderer/lib/rendererUtils.ts tests/rendererUtils.test.ts
git commit -m "feat: add renderer thumb path inference helper with tests"
```

---

### 任务 3：主进程生成缩略图 + ensure-thumbs IPC

**文件：**
- 修改：`src/main/ipc/index.ts`（`music:read-metadata` 约 1856-1876 行；新 handler 插在 `music:read-metadata` handler 之后）
- 依赖：任务 1 的 `thumbPathFor`；`nativeImage` 已在文件顶部导入（第 7 行）

- [ ] **步骤 1：read-metadata 中生成缩略图并返回 thumbPath**

将 `src/main/ipc/index.ts` 中封面保存代码段（约 1865-1871 行）扩展为：

```ts
                        const coverExt = pic.format === 'image/png' ? '.png'
                            : pic.format === 'image/webp' ? '.webp' : '.jpg';
                        const hash = crypto.createHash('sha256').update(pic.data).digest('hex').slice(0, 32);
                        const coverFileName = hash + coverExt;
                        const coverFullPath = path.join(getMusicCoversDir(), coverFileName);
                        try {
                            await fs.promises.mkdir(getMusicCoversDir(), { recursive: true });
                            await fs.promises.access(coverFullPath).catch(async () => {
                                await fs.promises.writeFile(coverFullPath, pic.data);
                            });
                            coverPath = coverFullPath;
                            // 缩略图：96×96 JPEG，列表项使用（控制条大封面仍用原图）
                            const thumbPath = thumbPathFor(coverFullPath);
                            try {
                                const img = nativeImage.createFromBuffer(pic.data);
                                if (!img.isEmpty()) {
                                    const thumbBuf = img.resize({ width: 96, height: 96, quality: 'good' }).toJPEG(85);
                                    await fs.promises.mkdir(path.dirname(thumbPath), { recursive: true });
                                    await fs.promises.access(thumbPath).catch(() => fs.promises.writeFile(thumbPath, thumbBuf));
                                }
                            } catch (e: any) {
                                console.warn('[Music] 缩略图生成失败:', e.message);
                            }
                        } catch (e: any) {
                            console.warn('[Music] 封面保存失败:', e.message);
                        }
```

- [ ] **步骤 2：返回 metadata 增加 thumbPath**

将 `music:read-metadata` 的返回值（约 1884-1895 行）扩展为：

```ts
        return {
            success: true,
            metadata: {
                filePath,
                title,
                artist,
                album,
                duration,
                coverPath,
                thumbPath: coverPath ? thumbPathFor(coverPath) : '',
                size: st.size
            }
        };
```

- [ ] **步骤 3：新增 music:ensure-thumbs handler**

在 `music:read-metadata` handler 结束之后插入：

```ts
    // 79b. 补齐已有封面缩略图（幂等：已存在跳过；供音乐 tab 激活时后台触发）
    ipcMain.handle('music:ensure-thumbs', tryWrap(async () => {
        const coversDir = getMusicCoversDir();
        let entries: fs.Dirent[];
        try {
            entries = await fs.promises.readdir(coversDir, { withFileTypes: true });
        } catch (_) {
            return { success: true, generated: 0, skipped: 0 };
        }
        let generated = 0, skipped = 0;
        for (const ent of entries) {
            if (!ent.isFile()) continue; // 跳过 thumb 子目录等
            const src = path.join(coversDir, ent.name);
            const thumb = thumbPathFor(src);
            try {
                await fs.promises.access(thumb);
                skipped++;
                continue;
            } catch (_) { /* 缩略图缺失，需生成 */ }
            try {
                const buf = await fs.promises.readFile(src);
                const img = nativeImage.createFromBuffer(buf);
                if (img.isEmpty()) { skipped++; continue; }
                const thumbBuf = img.resize({ width: 96, height: 96, quality: 'good' }).toJPEG(85);
                await fs.promises.mkdir(path.dirname(thumb), { recursive: true });
                await fs.promises.writeFile(thumb, thumbBuf);
                generated++;
            } catch (e: any) {
                console.warn('[Music] 缩略图生成失败:', src, e.message);
                skipped++;
            }
        }
        return { success: true, generated, skipped };
    }));
```

- [ ] **步骤 4：文件顶部导入 thumbPathFor**

在 `src/main/ipc/index.ts` 的 import 区加入：

```ts
import { thumbPathFor } from '../lib/coverThumbs.js';
```

- [ ] **步骤 5：类型检查**

运行：`npx tsc --noEmit -p tsconfig.main.json`
预期：无错误输出，退出码 0

- [ ] **步骤 6：Commit**

```bash
git add src/main/ipc/index.ts
git commit -m "feat: generate cover thumbnails in read-metadata and add ensure-thumbs IPC"
```

---

### 任务 4：preload 暴露 musicEnsureThumbs

**文件：**
- 修改：`src/preload/preload.ts`（约 146 行 `musicSaveFolders` 之后）

- [ ] **步骤 1：添加 API 暴露**

在 `src/preload/preload.ts` 中 `musicSaveFolders` 一行之后添加：

```ts
    musicEnsureThumbs: makeInvoke('music:ensure-thumbs'),
```

- [ ] **步骤 2：类型检查**

运行：`npx tsc --noEmit -p tsconfig.main.json`
预期：无错误输出，退出码 0

- [ ] **步骤 3：Commit**

```bash
git add src/preload/preload.ts
git commit -m "feat: expose musicEnsureThumbs in preload bridge"
```

---

### 任务 5：音乐列表改用缩略图 + 回退 + 触发补齐

**文件：**
- 修改：`src/renderer/tabs/musicTab.ts`（import 区、renderPlaylist 封面创建约 895-900 行、init() 内触发补齐）
- 依赖：任务 2 的 `inferThumbPath`

- [ ] **步骤 1：导入 inferThumbPath**

将 `src/renderer/tabs/musicTab.ts` 第 9 行：

```ts
import { debounce } from '../lib/rendererUtils.js';
```

改为：

```ts
import { debounce, inferThumbPath } from '../lib/rendererUtils.js';
```

- [ ] **步骤 2：列表项封面改用缩略图 + 两级 onerror 回退**

将 `src/renderer/tabs/musicTab.ts` renderPlaylist 中封面创建段（约 895-900 行）：

```ts
        const cover = document.createElement('img');
        cover.className = 'music-item-cover';
        cover.src = buildCoverUrl(t.coverPath) || defaultCoverSvg();
        cover.alt = '';
        cover.loading = 'lazy';
        cover.onerror = () => { cover.src = defaultCoverSvg(); };
```

改为：

```ts
        const cover = document.createElement('img');
        cover.className = 'music-item-cover';
        cover.alt = '';
        cover.loading = 'lazy';
        cover.decoding = 'async';
        // 列表项优先缩略图（96×96），控制条大封面仍用原图
        const thumbPath = t.thumbPath || inferThumbPath(t.coverPath);
        const coverUrl = thumbPath ? buildCoverUrl(thumbPath) : '';
        const origUrl = t.coverPath ? buildCoverUrl(t.coverPath) : '';
        cover.dataset.origUrl = origUrl || '';
        cover.src = coverUrl || origUrl || defaultCoverSvg();
        cover.onerror = () => {
            // 两级回退：缩略图失败 → 原图；原图失败 → 默认 SVG
            if (cover.dataset.origUrl && cover.src !== cover.dataset.origUrl) {
                cover.src = cover.dataset.origUrl;
            } else {
                cover.src = defaultCoverSvg();
            }
        };
```

- [ ] **步骤 3：init() 末尾后台触发缩略图补齐**

在 `src/renderer/tabs/musicTab.ts` 的 `init()` 函数末尾（`applyVolume(); updateVolumeUI();` 之后、`init` 的 catch 之前）插入：

```ts
    // 后台补齐已有封面缩略图（幂等；补齐完成后重渲染列表以使用缩略图）
    if (window.api && window.api.musicEnsureThumbs) {
        window.api.musicEnsureThumbs().then((r: any) => {
            if (r && r.success && r.generated > 0) {
                renderPlaylist();
            }
        }).catch(() => {});
    }
```

- [ ] **步骤 4：类型检查**

运行：`npx tsc --noEmit -p tsconfig.renderer.json`
预期：无错误输出，退出码 0

- [ ] **步骤 5：Commit**

```bash
git add src/renderer/tabs/musicTab.ts
git commit -m "feat: use cover thumbnails in music list with fallback chain"
```

---

### 任务 6：主窗口恢复后台节流（CPU 优化）

**文件：**
- 修改：`src/main/main.ts`（第 189 行）

- [ ] **步骤 1：删除 backgroundThrottling: false**

将 `src/main/main.ts` buildWindowOptions 中（约 184-190 行）：

```ts
        webPreferences: {
            preload: path.join(__dirname, 'preload.js'),
            contextIsolation: true,
            nodeIntegration: false,
            sandbox: true,
            backgroundThrottling: false,
        },
```

改为：

```ts
        webPreferences: {
            preload: path.join(__dirname, 'preload.js'),
            contextIsolation: true,
            nodeIntegration: false,
            sandbox: true,
        },
```

- [ ] **步骤 2：类型检查**

运行：`npx tsc --noEmit -p tsconfig.main.json`
预期：无错误输出，退出码 0

- [ ] **步骤 3：Commit**

```bash
git add src/main/main.ts
git commit -m "fix: restore background throttling to reduce CPU when minimized"
```

---

### 任务 7：后台节流验证清单（③ 通过/回退判定）

**文件：** 无代码改动（验证）。若验证失败，执行备选方案（见设计文档 4.4，另行计划）。

- [ ] **步骤 1：构建并启动**

运行：`npm run build:ts`
预期：构建成功，无错误。

启动：`npx electron . --remote-debugging-port=9333`（后台）

- [ ] **步骤 2：验证音乐后台播放**

操作：音乐 tab 播放一首 → 最小化窗口（CDP：`window.__ = 1` 后 `document.querySelector('.music-item')?.click()`，再用 PowerShell `Minimize` 或 CDP 无法最小化时用 PowerShell 调用 `$p.MainWindowHandle` + ShowWindow）。
预期：等待 15s 后检查 `document.querySelector('.music-item.playing')` 存在且 `howler` 音频继续（renderer 内 `new Audio()` 元素 `paused === false`），声音持续。

判定：通过 → 保留任务 6 改动；失败（音频停止）→ 回退任务 6 的 commit（`git revert`），并在交付说明中标注。

- [ ] **步骤 3：验证闹钟准时性**

操作：通过应用 UI 设一个 60 秒后的闹钟 → 立即最小化窗口 → 计时 60s → 检查闹钟是否在设定时间 ±5s 内响铃（观察 renderer 是否触发响铃：CDP 轮询 `document.querySelector('.alarm-ringing')` 或日志）。
预期：闹钟在 ±5s 内触发。

判定：通过 → 保留；失败（延迟 ≥30s）→ 回退任务 6，并在交付说明中标注「闹钟在后台节流下延迟，保留 backgroundThrottling: false 或需主进程兜底（另行计划）」。

- [ ] **步骤 4：验证时钟恢复**

操作：最小化 60s 后恢复窗口，检查界面时钟显示与系统时间一致（误差 ≤2s）。
预期：恢复后 1s 内时钟刷新正确。

判定：通过 → 任务 6 最终保留。

---

### 任务 8：回归 + 内存收益复测

**文件：** 复用 `.reasonix/tmp/cdp.mjs`（不入 git）

- [ ] **步骤 1：全量类型检查与单测**

运行：`npm run typecheck && npm test`
预期：typecheck 无错误；vitest 全部 PASS（含新增 coverThumbs/rendererUtils 测试）

- [ ] **步骤 2：清理旧缩略图并验证 ensure-thumbs 幂等**

操作：确认 `%APPDATA%\便签\music\covers\thumb\` 生成，且第二次触发 `music:ensure-thumbs` 返回 `generated: 0`（跳过全部）。
预期：首次生成若干、二次全跳过。

- [ ] **步骤 3：基线复测（与设计文档 §2 数据对比）**

操作：确保 `%APPDATA%\便签\music\playlist.json` 为真实数据（843 首），启动应用后运行 `.reasonix/tmp/cdp.mjs`。
预期：renderer（音乐 tab）从 447M 降至 ≤320M；任务管理器单条目相应下降。记录实测值到交付说明。

- [ ] **步骤 4：功能回归抽查**

操作：手动/CDP 验证——切歌、拖动进度条、收藏/取消收藏、搜索过滤、拖拽排序、控制条封面显示原图。
预期：全部正常，UI 视觉与改动前一致。

- [ ] **步骤 5：Commit（若步骤 2-4 有代码修正）**

```bash
git add -A
git commit -m "fix: tune thumbnail ensure or regression fixes"
```

---

## 自检记录

- **规格覆盖度：** 设计文档 §3（缩略图：read-metadata 生成 → 任务 3；ensure-thumbs 补齐 → 任务 3/5；渲染层引用与回退 → 任务 5；协议层无需改动，已确认 covers/thumb 子目录通过现有校验）；§4（后台节流 → 任务 6/7）；§6 成功标准 → 任务 8 复测。无遗漏。
- **占位符扫描：** 无 TODO/待定；所有代码块完整。
- **类型一致性：** `thumbPathFor`（任务 1，主进程）与 `inferThumbPath`（任务 2，渲染层）职责分离，命名一致；`musicEnsureThumbs`（任务 4 preload）与 `music:ensure-thumbs`（任务 3 IPC）通道一致；metadata `thumbPath` 字段在任务 3 返回、任务 5 消费，一致。
